{
  "name": "win11-vue3",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "frontend-serve": "pnpm --filter frontend serve",
    "build": "vue-tsc -b && vite build",
    "preview": "vite preview",
    "prepare": "husky",
    "test": "vitest"
  },
  "dependencies": {
  },
  "devDependencies": {
    "@antfu/eslint-config": "^4.1.1",
    "@types/node": "^24.0.15",
    "eslint": "^9.20.0",
    "husky": "^9.1.7",
    "lint-staged": "^15.4.3",
    "typescript": "^5.2.2"
  },
  "lint-staged": {
    "*.{js,jsx,ts,tsx,vue}": [
      "eslint --fix"
    ]
  }
}
